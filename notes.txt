Build docker lambda, command line

$ docker build --file=./Dockerfiles/lambda/Dockerfile . -t lambda:latest

Lambda deploy

$ cdk deploy my-tiler-lambda-dev

Executar servidor localmente

$ uvicorn titiler.main:app --reload
